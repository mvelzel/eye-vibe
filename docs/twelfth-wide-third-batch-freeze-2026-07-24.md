# Twelfth wide horizon — final wide batch freeze

## Scope

Ten independently motivated constructions have now been screened. This final
wide batch covers the remaining mechanism families without reopening the
failed models. Two receive new finite tests; three are bounded by existing
exact audits or an identifiability proof.

Recent read-only Discord review supplies no independently selected in-game
decoder interface. The only community-identified in-game link to deck ciphers
is the shared number 83 (Eye alphabet and procedural gun-name count).

## Frozen tests

| Lane | Exact action | Promotion gate |
|---|---|---|
| **L. Spectral organization** | Form the 83×83 directed body-transition support. Compute its leading singular-energy fraction by deterministic power iteration. Compare with degree-preserving directed edge swaps that keep every in/out degree and no-loop constraint. | A corrected tail below `.01` is only a screen; promotion also requires replication in a separately built panel-alignment operator. |
| **M. Five-ary convolutional syndrome** | On raw directions after each established deepest prefix-leaf exit, enumerate every normalized linear parity filter over `F5` with memory one through three (lengths 2–4, nonzero last tap). Select taps on the P-header panels and score zero syndromes on the six Q panels. | Refit the complete 124-filter family after accepted-trigram/row-pair/parity shuffles. Promotion requires heldout corrected tail below `.01`; in-sample sparsity fails. |
| **N. Packed-storage bitplanes** | Audit the exact storage reconstruction and previous capacity-bit test rather than rescan 64 arbitrary bit positions. Record whether any engine- or asset-selected bit index exists. | Without an independent selector, no new bitplane scan is admissible. A selected bitplane would still need a heldout panel prediction. |
| **O. Conventional-mode necessary conditions** | Prove the unknown-plaintext identifiability boundary for reused-keystream stream modes and arbitrary keyed permutations: for any ciphertext corpus, choosing the unknown plaintext/state can realize the observed shared prefixes and rejoins. Inventory only conditions that become testable with a known plaintext or nonce. | Advance only if a key-free ciphertext contradiction exists. Merely naming CTR/CBC/OFB/sponge does not constrain unknown plaintext. |
| **P. Executable clue interface** | Consolidate the selected Eye renderer, procedural 83-of-101 branch, Gate Guardian, gun-name, and Lua arithmetic data-flow audits with the latest Discord review. Do not mine new native literals. | Advance only an authored path that predicts a frozen Eye quantity beyond the number used to select it. The number 83 alone fails. |

## Stop rule

After L–P, step back again. If no lane advances, the honest bottleneck is not
another unconstrained representation scan: it is an external clue, known
plaintext, or new method for arbitrary hidden-state cipher inference. Practice
ciphers 3 and 4 remain separate active opportunities to acquire such a method.
