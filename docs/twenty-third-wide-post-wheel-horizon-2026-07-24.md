# Twenty-third wide horizon: new discriminators after the wheel failure

## Boundary

Two uses of the Earthquake irregular row are now closed:

1. static selection on the final anchor slices;
2. the exact Cessation-derived visual-row sampler.

The final self-describing anchor record remains positive.  The correct response
is neither to rotate the failed tape nor to add phases.  This horizon widens
across different evidence carriers before another branch receives depth.

A read-only check of the current Discord channels adds three provenance notes:

- the proposed *Aholla itkijä* syllable pair had already been reproduced and
  rejected here because it edits the public source and fails the later Eye
  equality runs;
- the tutorial-stone highlight anomaly was traced in the same discussion to
  background and glow layers loading at different times, and its discoverer
  judged the glitch likely meaningless;
- Lquid's newly shared Cessation practice work folder supplies a real
  terminal-aware traversal method, but the exact prospective Eye transfer is
  now null-typical with tail `.955042017`.

No Discord messages, reactions, or calls were made.

## Wide portfolio

| Lane | Object | First bounded discriminator | Promotion boundary |
|---|---|---|---|
| **A. Desert paired-record quotient** | The equally weighted diamond/ovals/`¡!¡` assets specify the historical paired-trigram orders `y=021`, `x=120`; `x²+y` is a lossy 19-symbol quotient. | Reset the exact transform at each of the seven fixed nonliteral context records. Score literal output agreement only on changed input pairs, with the first four contexts descriptive and the last three primary. Compare with global `0..82` relabelings that preserve the complete equality skeleton. | Promote only below `.01` on held out. Do not launch substitution or language searches. |
| **B. First/second marker-row records** | The final marker row is a closed typed difference/pointer record; the first two rows may be different record types rather than cipher keys. | Freeze the minimum field grammar learned from the final row, then ask it to predict one unused first/second-row slot, target rank, or pointer without changing operators. | The final row may train field semantics, not numeric constants. No new operator per row. |
| **C. Rune-to-eye/component mapping** | Earthquake outer/inner rune cycles may select the five eye directions and six renderer/control symbols. | Search asset animation order, authored gap direction, particle emission order, and code paths for one mapping fixed without Eye data. | Bare cardinality `5/6` is insufficient; do not scan `5!` or `6!` and report the best. |
| **D. Practice ciphers 3 and 4** | The unresolved sdlwdr exercises may teach a representation or state invariant unavailable from the Eyes alone. | Resume Cipher 3 at its remaining third-layer action and Cipher 4 at the recovered cyclic outer layer, requiring full re-encryption or author-confirmable plaintext. | Practice solutions transfer methods, not their plaintext, keys, or game easter eggs. |
| **E. Gate binary topology** | The later Gate independently has two side inputs, upper control, and center result, matching the final Eye interface `77+33 -> 27 mod83`. | Derive one Gate mask or branch rule entirely from its sprites/Lua, then predict a frozen Eye field not used to recognize the arity. | Topology remains corroborative only. Do not fit the dossier's Type4/Type6 rules to Eye numbers. |
| **F. Version-differential clue** | A later game revision may deliberately explain a 2020 construction. | Date and diff the first Gate, Cessation, Earthquake, and renderer-adjacent asset forms; promote only a semantic edit that selects an operation. | Unchanged early existence proves eligibility, not intent. Number coincidence alone fails. |
| **G. Source/edit ancestry** | The panels may be edited descendants of a common worksheet, verse, or prose template, with edit operations carrying state. | Fit an equality-template edit script on one natural sibling family and predict edit columns in another. | Chronology applies to borrowed source material. Developer-created ideas may predate their public addition. |
| **H. Anchor-record coding theory** | The gap-11 loop and its pointer may form a self-synchronizing or error-detecting record rather than plaintext. | Enumerate only standard length-12 single-repeat/check formats whose field roles are selected by the already decoded header slots; demand a first/second-row prediction. | A code-family label is not evidence unless it predicts an unseen field. |

## Cost-adjusted order

Lane A comes first because its representation, component orders, train/held-out
split, and equality-skeleton null are already finite.  It can be closed or
promoted without new asset interpretation.  Lanes B and D follow: B attacks
the strongest Eye result directly, while D increases method competence on
known-solvable material.  C/E/F remain acquisition-gated; G/H stay behind
them because their first honest model still needs a sharper finite grammar.

## Discipline

Every implemented lane gets its own freeze before the real statistic is read.
A failure returns the portfolio to breadth.  A survivor earns only the next
prediction, not an immediate plaintext claim.

