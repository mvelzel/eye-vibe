# Seventh wide funnel — dynamic transforms and self-indexing

## Purpose

This pass starts wide again after the direct factoradic-body interfaces and the
giant-dollar audit. It does not deepen the most picturesque survivor. Six
mechanisms that make materially different predictions receive one cheap test
each before any is extended.

“Project-new” below means absent from the public Discord material and local
research ledger checked so far. It cannot prove that nobody has considered an
idea privately. The tests use no plaintext crib, English score, Gate number,
or fitted letter map.

## Frozen facts and controls

- The nine headers are ordinary lexicographic `S6` ranks over
  `[center,up,right,down,left,newline]`; their finite type/coset structure is
  independently reproduced.
- Applying those permutations directly to body glyphs, products, quotients,
  cosets, running states, or newline positions was negative.
- The marker layer independently produced an exact cyclic BWT object, so BWT
  vocabulary is admissible without claiming that the bodies are BWT rows.
- The bodies have a fixed equality/prefix tree and no adjacent equal values.
  A control must preserve those facts whenever they can affect its statistic.
- Canonical numeric labels `0..82`, base-five digits, visual rows, and the
  modulus-101 embedding are developer-authored. A global permutation of body
  labels is the matched control for a proposed relation to those absolute
  labels; the headers remain frozen.
- For statistics determined only by occurrence/equality structure, use the
  established prefix-tree-preserving positional shuffle instead.
- Every lane reselects its small stated convention family inside every
  control. Promotion requires a corrected tail below `0.01` and a second
  held-out or independently authored consequence.

## Wide portfolio before depth

| Lane | Concrete interpretation | Cheap necessity test | Prior-work boundary |
|---|---|---|---|
| **A. Header-ordered BWT→MTF** | A header supplies the collation order for the exact six renderer symbols; the body is a transform/compression object rather than six-symbol substitution. | Rebuild each body tape with authored newlines, append one fixed sentinel, BWT-sort under the header order or its inverse, then move-to-front. Select the smaller total BWT run count and compare with global body-label relabelings. | The marker BWT acts only on nine header digits; the rejected moving-delimiter lane substituted symbols rather than using them as a sort order. |
| **B. Six-token block transposition** | The `S6` header permutes positions in consecutive six-trigram records, not eye symbols or group elements. | Reorder complete body blocks by the header or inverse and select adjacent-equality plus repeated-bigram gain. Compare with prefix-tree-preserving positional shuffles. | No previous S6 test permuted body *positions* in fixed six-token blocks. |
| **C. Hidden-gap derivative in `Z101`** | The visible interval `0..82` sits inside a 101-state cycle; an adjacent move may cross one or more of the 18 invisible states and emit a side channel. | Follow the unique shortest signed mod-101 step and record whether it intersects `83..100`. Test binary run/compression structure against global relabelings. | Earlier `Z101` tests summed values or fitted affine walks; none serialized crossings of the exact missing interval. |
| **D. Sequential eye-change masks** | Information is carried by *which* of the three eyes changes between adjacent trigrams, not by the values themselves. | Emit the three coordinate-change bits per transition and measure fixed byte compression/serial structure, with one companion ternary comparison-sign channel and family reselection under global relabelings. | The static Hamming-distance histogram was negative, but it discards the coordinate identities and their order in time. |
| **E. Temporal occurrence-pointer tape** | Each repeated label points to its next occurrence; the intervening distance, rather than the label, is the payload. | Serialize forward next-occurrence distances for every repeat event and measure entropy/compression against prefix-tree-preserving positional shuffles. | LRU stack distance only tested whether an eight-entry cache can reproduce labels; it did not test distances as a separate data tape. |
| **F. Ordinal/factoradic self-indexing** | A header rank describes the relative numeric order of a natural six-symbol body sample. | Test the first six distinct body values and all distinct sliding six-windows against the header permutation and inverse; calibrate the complete two-convention family with global relabelings. | Products and quotients treat values as `S6` group elements. This lane treats six body positions as one Lehmer/ordinal record. |

The six lanes are intentionally heterogeneous: alphabet transform, positional
transposition, modular side channel, coordinate derivative, equality-only
pointer tape, and ordinal self-description. A hit in one does not license
parameter expansion in the others.

## Deferred independent lanes

The remaining sixth-expansion ideas are still on the ledger and are not
replaced by this portfolio:

- synchronizing/error-detecting properties of the 83-of-125 codebook;
- without-replacement 26/83-card packets;
- reconvergence cocycles on the equality graph;
- straight-line phrase grammar beyond copied prefixes;
- only historically eligible Noita PRNG trajectories;
- model-based range coding using an independently authored frequency table.

They become a second batch only if all A–F close or if a new external selector
promotes one. This prevents a result-free first batch from silently deleting
the broader queue.

## Stop rules

- Lane A must beat controls on a collation-sensitive BWT statistic. A readable
  preview selected after the fact is not sufficient.
- Lane B must create structure beyond the known copied prefixes. One accidental
  double or a favourable six-block phase is insufficient; phase zero at the
  header/body boundary is frozen.
- Lanes C and D must show sequential structure, not merely reproduce a biased
  marginal bit count or the known Hamming histogram.
- Lane E must beat the full equality-aware positional null; a short pointer
  alphabet caused only by label frequency does not count.
- Lane F needs several exact header-record matches or a corrected rare excess;
  one of roughly one thousand eligible windows matching by chance does not
  count.

If no lane crosses `0.01`, record all six negatives and widen to the deferred
batch or return to practice-cipher method calibration. Do not rescue the best
ordinary p-value with offsets, arbitrary alphabets, or per-message choices.

## Results

All six frozen forms are negative. The reported tails reselect the stated
convention family inside every one of 2,000 controls:

| Lane | Observed selected statistic | Corrected matched-control tail | Decision |
|---|---:|---:|---|
| A. header-ordered BWT→MTF | 2,523 runs over 3,176 symbols, inverse header order | `1101/2001 = 0.550225` lower | stop |
| B. six-token transposition | 10 adjacent doubles, 106 repeated bigrams, header order | `1551/2001 = 0.775112` upper | stop |
| C. `Z101` gap derivative | 132 compressed bytes, 250 runs, 173/1,018 gap bits | `1800/2001 = 0.899550` lower | stop |
| D. eye-change sequence | 385 compressed bytes, 711 runs, change-mask channel | `1465/2001 = 0.732134` lower | stop |
| E. occurrence pointers | 413 compressed bytes, entropy 6.03924, 469 events | `71/2001 = 0.035482` lower | stop |
| F. ordinal/factoradic records | one sliding match among 873 windows; no initial match | `1807/2001 = 0.903048` upper | stop |

### A: the natural compression synthesis fails

This was the most strongly motivated new lane because it joined three
independent facts: the six-symbol factoradic header, the real renderer newline,
and the marker BWT. The implementation removes the spatially separated header
eyes correctly—two from the first top row and one from the first bottom row—so
the header controls rather than contaminates its body tape. It then appends a
unique fixed sentinel, sorts under the header or inverse order, and performs
ordinary sentinel-first move-to-front coding.

The inverse route gives 2,523 BWT runs and 653 MTF zeros. This is exactly
ordinary under equality/prefix-preserving global relabelings. The result
rejects the compact BWT→MTF pipeline, not every transform that could consume a
six-symbol collation order.

### B–D: no positional or derivative side channel

Permuting complete six-token body blocks creates ten adjacent doubles, but
matched positional controls usually create at least as much combined double
and bigram structure. Treating `0..82` as the visible interval of `Z101`
produces 173 transitions whose unique shortest path crosses the hidden
`83..100` interval. Their binary order is less, not more, compressible than
most relabeled controls. Likewise, retaining the temporal order and identity
of the three changing eyes adds no signal beyond the previously ordinary
Hamming marginal.

### E: refined near miss closes

Forward next-occurrence distances form 469 events with 92 distinct byte
values. The first 500-control pass gave `10/501 = 0.019960`, the only result
near the threshold. A predeclared refinement to 2,000 controls moved it away
from promotion, to `71/2001 = 0.035482`. No distance modulus, direction,
letter map, or compression model was added. The direct temporal-pointer tape
is closed; the broader typed allocation/cache idea remains possible only with
an external rule.

### F: headers do not self-index natural six-value samples

The first six distinct values match neither selected header convention in any
panel. Across 873 distinct sliding six-windows, the selected inverse route has
one match total, an entirely ordinary count. Thus the factoradic headers do
not describe these natural Lehmer records.

## Outcome

No lane earns depth. In particular, the factoradic header remains structural
metadata with no direct body consumer, and the genuine dollar mirror relation
does not alter that conclusion. The next breadth work should use the six
deferred independent lanes rather than parameterizing A–F:
codebook synchronization, without-replacement packets, equality-graph
cocycles, phrase grammar, eligible Noita RNGs, and externally selected range
coding.

Reproduction:

```bash
PYTHONPATH=src python3 scripts/run_seventh_wide_funnel.py \
  --controls 2000 --seed 20260722
```
