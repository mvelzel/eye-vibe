# Anchor-record slot/target follow-up — results

## Result

The omitted target-column relation passes its fully reselected matched
control:

```text
broad numeric-plus-rank corrected tail = 25/50001 = .0004999900
```

This is promoted as another field of the same final-row construction record.
It shares anchors and is not an independent p-value to multiply.

## Exact slot rule

For final component orders:

```text
E4  012
W4  021
E5  102
```

all six directed slot differences give:

```text
slot pair  output vector
0-1        77,27,6
0-2        27,77,33  <- exact markers
1-0         6,56,77
1-2        33,50,27
2-0        56,6,50
2-1        50,33,56
```

Thus the unique exact convention is:

```text
header = anchor[source] - anchor[remaining] mod 83
```

The control target in slot 1 is omitted from the arithmetic. Across panels,
those targets are `(1,2,0)`, exactly the ascending ranks of anchor values
`(75,81,48)`.

## Controls

The positive plant recovers anchors, rank column, unique slot pair `0-2`, and
the broad selector.

Across 50,000 new nuisance-preserving body shuffles:

```text
1640  have one clean gap-11 anchor in all three panels
 253  also have exact ascending target ranks (1,2,0)
   0  satisfy the exact numeric formula
   0  satisfy exact numeric and rank fields jointly
  24  survive broad gap/numeric/rank reselection
```

The targeted rank-only count gives a corrected global tail
`254/50001 = .005079898`; it is not surprising conditional on already having
three distinct anchors. Its evidentiary value comes from joining the
independently defined target column to the broadened numeric record. The broad
audit searches gaps `2..30`, permits every anchor ordering and header-edge
assignment, and accepts ascending or descending target ranks.

## Current final-row record

The marker component orders now have three linked consumers:

```text
slots 0 and 2       choose source-minus-remaining anchor operands
slot 1 column       gives anchor-value ranks (1,2,0)
W4 complete order   gives anchor-position ranks (0,2,1)
```

The full marker values then carry the resulting checks:

```text
77 + 33 = 27 mod 83
```

This is a compact self-describing record, not merely three generic checksum
bytes. It still lacks the selector semantics for base position 16 and gap 11,
and it does not expose the plaintext carried by the surrounding streams.
